package com.remind.memorylog.domain.SuggestionImage.repository;

import com.remind.memorylog.domain.SuggestionImage.Entity.SuggestionImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SuggestionImageRepository extends JpaRepository<SuggestionImage, Long> {
}
